#include <iostream>
#include "mylibrary.h"
/*
2.Write a program that declares an arbitrary function in a header file.
 The header file is called mylibrary.h
  Define a function inside the source file called mylibrary.cpp.
   The main function is inside a second source file called source.cpp file.
   Include the header in both source files and invoke the function.
*/
int main()
{
    sampleFunction();
    return 0;
}
